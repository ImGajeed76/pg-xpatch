-- pg_xpatch upgrade script: 0.2.0 -> 0.2.1
-- This is a bug-fix release with no schema changes.
-- The fixes are in the C code (TOAST support, sequence rollback, performance).

-- No SQL changes required for this upgrade.
